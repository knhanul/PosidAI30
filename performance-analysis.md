# 성능 분석

## 확인된 병목과 근거

### 목록 크기

이전 `/api/posts`는 최대 200건을 반환했다. 게시물 본문은 이미 `defer(Post.body_markdown)`으로 제외돼 있었지만, 많은 카드 메타데이터와 최대 200개 ID에 대한 집계가 한 요청에 포함됐다. 페이지를 먼저 결정하는 것이 대규모 데이터에서 안전하다.

### 좋아요/댓글 집계

`1895b35`는 상관 서브쿼리가 아니라 선택된 게시물 ID로 두 번의 `GROUP BY post_id`를 수행한다. 따라서 N+1이나 행마다 재평가되는 SubPlan은 없다. `post_likes`는 `(post_id,user_id)` 기본키로, `comments`는 `post_id` 인덱스로 접근한다. 댓글 모델에는 soft delete/private 컬럼이 없으므로 현재 집계는 존재하는 모든 댓글을 정확히 센다. 향후 soft-delete/private 상태가 추가되면 집계와 목록 모두 같은 조건을 적용하고 부분 인덱스를 추가해야 한다.

### NAS/WireGuard

목록 API는 NAS를 조회하지 않는다. 다만 실제 썸네일·이미지·다운로드는 FastAPI가 WebDAV에서 동기 스트리밍하며, 요청마다 새 httpx 클라이언트를 만든다. NAS 연결 불능 때 기존 단일 60초 timeout은 워커 스레드를 오래 점유할 수 있다. NAS 회선(약 10Mbps)은 원본 대용량 전송의 근본 한계다. 이 작업에서는 측정 없이 MTU/혼잡제어/keepalive를 변경하지 않았다.

### 리소스와 배포

Uvicorn은 명시적 worker 옵션이 없어 1 worker다. SQLAlchemy 기본 풀은 5+10이므로 1GiB 운영 서버에 비해 필요 이상으로 커질 수 있다. Compose는 이전에 Docker json-file 로그 회전 상한이 없었다.

## 선택하지 않은 대안

- 카운터 캐시: 현재 배치 집계는 4 SQL로 상수이며, 낮은 페이지 크기에서 충분하다. 카운터 캐시는 동시성, 백필, 복구 검증 복잡도가 더 크므로 도입하지 않았다.
- Redis/상시 공유 캐시: 1GiB 메모리에서 추가 상주 서비스 비용이 이점보다 크고, 현재 목록은 NAS I/O를 하지 않는다.
- Nginx 공용 파일 캐시: 인증·비공개 파일 정책과 캐시 키를 분리 검증하지 않은 상태에서 도입하면 정보 노출 위험이 있다.
- WireGuard 튜닝: 운영 터널 계측 결과가 없어 변경하지 않았다.
